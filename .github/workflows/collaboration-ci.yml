name: Collaboration CI

on:
  pull_request:
    paths:
      - ".github/workflows/collaboration-ci.yml"
      - "deploy/collaboration-private/**"
      - "packages/collaboration-*/**"
      - "packages/domains/collaboration/**"
      - "packages/domains/identity-access/**"
      - "infra/keycloak/**"
      - "docs/adr/**"
      - "docs/contexts/identity-access/**"
      - "docs/identity-access-pr78-acceptance-receipt.md"
      - "openspec/changes/README.md"
      - "openspec/changes/unify-user-device-collaboration/specs/user-device-identity/spec.md"
      - "packages/domains/content-space/src/contract.ts"
      - "packages/domain-sdk/**"
      - "test-fixtures/collaboration/e-content-space-portable.mjs"
      - "scripts/collaboration-*.mjs"
      - "scripts/build-collaboration-server-bundle*.mjs"
      - "scripts/domain-package-tarball-smoke.test.mjs"
      - "scripts/domain-packages.mjs"
      - "scripts/fixtures/identity-package-secrets-process.mjs"
      - "scripts/identity-access-docs.test.mjs"
      - "scripts/keycloak-*.mjs"
      - "scripts/publishable-package-version-audit.mjs"
      - "src/main/domain-*.ts"
      - "src/main/index.ts"
      - "src/main/principal-context*"
      - "src/main/capabilities/agent-tools*"
      - "src/main/capabilities/broker*"
      - "src/main/capabilities/identity-principal-transition.integration.test.ts"
      - "src/main/capabilities/registry*"
      - "src/main/ipc/register-app-ipc-handlers*"
      - "src/preload/index.ts"
      - "src/renderer/src/components/chat/WorkbenchTopBar*"
      - "src/renderer/src/dev/dev-sciforge-bridge.ts"
      - "src/renderer/src/lib/capability-client*"
      - "src/shared/capability-broker.ts"
      - "src/shared/sciforge-api.ts"
      - "package.json"
      - "package-lock.json"
  push:
    branches:
      - gui
    paths:
      - ".github/workflows/collaboration-ci.yml"
      - "deploy/collaboration-private/**"
      - "packages/collaboration-*/**"
      - "packages/domains/collaboration/**"
      - "packages/domains/identity-access/**"
      - "infra/keycloak/**"
      - "docs/adr/**"
      - "docs/contexts/identity-access/**"
      - "docs/identity-access-pr78-acceptance-receipt.md"
      - "openspec/changes/README.md"
      - "openspec/changes/unify-user-device-collaboration/specs/user-device-identity/spec.md"
      - "packages/domains/content-space/src/contract.ts"
      - "packages/domain-sdk/**"
      - "test-fixtures/collaboration/e-content-space-portable.mjs"
      - "scripts/collaboration-*.mjs"
      - "scripts/build-collaboration-server-bundle*.mjs"
      - "scripts/domain-package-tarball-smoke.test.mjs"
      - "scripts/domain-packages.mjs"
      - "scripts/fixtures/identity-package-secrets-process.mjs"
      - "scripts/identity-access-docs.test.mjs"
      - "scripts/keycloak-*.mjs"
      - "scripts/publishable-package-version-audit.mjs"
      - "src/main/domain-*.ts"
      - "src/main/index.ts"
      - "src/main/principal-context*"
      - "src/main/capabilities/agent-tools*"
      - "src/main/capabilities/broker*"
      - "src/main/capabilities/identity-principal-transition.integration.test.ts"
      - "src/main/capabilities/registry*"
      - "src/main/ipc/register-app-ipc-handlers*"
      - "src/preload/index.ts"
      - "src/renderer/src/components/chat/WorkbenchTopBar*"
      - "src/renderer/src/dev/dev-sciforge-bridge.ts"
      - "src/renderer/src/lib/capability-client*"
      - "src/shared/capability-broker.ts"
      - "src/shared/sciforge-api.ts"
      - "package.json"
      - "package-lock.json"

permissions:
  contents: read

concurrency:
  group: collaboration-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Audit publishable package versions
        if: github.event_name == 'pull_request'
        run: |
          git fetch --no-tags origin \
            "${{ github.base_ref }}:refs/remotes/origin/${{ github.base_ref }}"
          node scripts/publishable-package-version-audit.mjs \
            --base "origin/${{ github.base_ref }}"

      - name: Check contracts and package boundaries
        run: npm run collaboration:typecheck

      - name: Run full application typecheck
        run: npm run typecheck

      - name: Run lint
        run: npm run lint

      - name: Run collaboration production-path tests
        run: npm run collaboration:test

      - name: Validate private acceptance driver
        run: npm run collaboration:acceptance:test

      - name: Test immutable collaboration release builder
        run: npm run collaboration:bundle:test

      - name: Check private deployment shell syntax
        run: bash -n deploy/collaboration-private/scripts/*.sh deploy/collaboration-private/postgres-init/*.sh

      - name: Verify private deployment security policy
        run: deploy/collaboration-private/scripts/static-policy-test.sh

      - name: Run generic capability boundary tests
        run: |
          npx vitest run \
            src/main/capabilities/broker.test.ts \
            src/main/capabilities/agent-tools.test.ts \
            src/renderer/src/lib/capability-client.test.ts

      - name: Run identity package and persistence tests
        run: |
          npm --workspace @sciforge/collaboration-identity test
          npm --workspace @sciforge/domain-identity-access test
          npx vitest run src/main/domain-package-storage.test.ts

      - name: Run Host identity boundary tests
        run: |
          npx vitest run \
            src/main/ipc/register-app-ipc-handlers.test.ts \
            src/renderer/src/components/chat/WorkbenchTopBar.test.ts

      - name: Run domain SDK and Principal transition tests
        run: |
          npm run domain-sdk:test
          npx vitest run \
            src/main/principal-context.test.ts \
            src/main/capabilities/identity-principal-transition.integration.test.ts

      - name: Verify Keycloak realm contract
        run: npm run identity:keycloak:check

      - name: Verify generated composition and capabilities
        run: |
          npm run domain-packages:generate
          git diff --exit-code
          npm run domain-packages:check
          npm run smoke:domain-packages:tarball
          npm run capability:check

      - name: Build deployable collaboration server
        run: npm --workspace @sciforge/collaboration-server run build

      - name: Verify server package contents
        run: npm pack --workspace @sciforge/collaboration-server --dry-run

  keycloak-macos-bash32:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Verify Keycloak contract with macOS system Bash 3.2
        shell: /bin/bash {0}
        run: |
          printf 'macOS system Bash: %s\n' "$BASH_VERSION"
          [[ "${BASH_VERSINFO[0]}.${BASH_VERSINFO[1]}" == "3.2" ]]
          export SCIFORGE_TEST_BASH=/bin/bash
          npm run identity:keycloak:check

  portable-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17.6-alpine
        env:
          POSTGRES_DB: postgres
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: sciforge-ci-only
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d postgres"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build public collaboration contracts
        run: npm --workspace @sciforge/collaboration-contracts run build

      - name: Verify v1 to current migration and portable artifact round trip
        env:
          SCIFORGE_POSTGRES_V5_INTEGRATION: "1"
          SCIFORGE_POSTGRES_V5_ADMIN_URL: postgresql://postgres:sciforge-ci-only@127.0.0.1:5432/postgres
        run: npm --workspace @sciforge/collaboration-server test -- --run src/postgres-v5.integration.test.ts
